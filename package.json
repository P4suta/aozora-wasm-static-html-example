{
  "name": "aozora-wasm-static-html-example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "node src/build.mjs",
    "test": "node --test",
    "check": "npm test && npm run build"
  },
  "dependencies": {
    "aozora-wasm": "0.5.0"
  },
  "devDependencies": {
    "parse5": "8.0.0"
  }
}
